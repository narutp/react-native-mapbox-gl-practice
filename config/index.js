import {
    APP_VERSION,
    MAPBOX_ACCESS_TOKEN,
    MAPBOX_STYLE_URL,
    MAPBOX_TILE_JSON,
    MAP_API_KEY,
    MAP_APP_ID,
    CLIENT_ID,
    CLIENT_SECRET,
    GATEWAY_CLIENT,
    COMPANY_LOGO,
  } from 'react-native-dotenv'
  
  const config = {
    APP_VERSION,
    MAPBOX_ACCESS_TOKEN,
    MAPBOX_STYLE_URL,
    MAPBOX_TILE_JSON,
    MAP_API_KEY,
    MAP_APP_ID,
    CLIENT_ID,
    CLIENT_SECRET,
    GATEWAY_CLIENT,
    COMPANY_LOGO,
  }
  
  export default config